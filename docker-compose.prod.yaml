services:
  db:
    image: postgres:16.0-alpine
    environment:
      POSTGRES_USER: debug
      POSTGRES_PASSWORD: debug_fduecn2025
      POSTGRES_DB: debug
    volumes:
      - pgdata:/var/lib/postgresql/data

  django:
    build:
      context: .
      dockerfile: compose/prod/Dockerfile
    command: /start
    volumes:
      - .:/app
      - ./static_cdn:/app/static_cdn
      - ./media:/app/media
      - ./logs:/app/logs/
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      DJANGO_SECRET_KEY: django-insecure-7g8jyz6uvf)+tbps9v&w)r^n&939ftgle(qr2!0bo18)a1@w2
      POSTGRES_USER: debug
      POSTGRES_PASSWORD: debug_fduecn2025
      POSTGRES_DB: debug
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      ALLOWED_HOSTS: "109.172.38.117"
      CSRF_TRUSTED_ORIGINS: "http://109.172.38.117"
      DJANGO_SETTINGS_MODULE: config.settings.prod
      EMAIL_BACKEND: "django.core.mail.backends.smtp.EmailBackend"
      EMAIL_HOST: "smtp.gmail.com"
      EMAIL_HOST_USER: "wkenesary@gmail.com"
      EMAIL_HOST_PASSWORD: "irta muie idlg kzds"
      EMAIL_PORT: 587

volumes:
  pgdata:
