services:
  pg_tron:
    container_name: pg_tron
    image: postgres:17.0-alpine
    environment:
      POSTGRES_USER: debug
      POSTGRES_PASSWORD: debug_fduecn2025
      POSTGRES_DB: debug
    ports:
      - 5432:5432
    volumes:
      - pgdata_tronline:/var/lib/postgresql/data

volumes:
  pgdata_tronline:
